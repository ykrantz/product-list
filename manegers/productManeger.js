// CRUD

export function createProduct() {
  DB_createProduct();
  //    TODO: when aading, do i need to add to db  and then to read the details from server or to add simoltany the producr?
  //  TODO: can i use name parameter in code?
}

export function readProduct() {}
export function updateProduct() {}
export function deleteProduct() {}
